export enum Level {
  UNIVERSITY = 'Вузовский',
  CITY = 'Городской',
  REGION = 'Региональный',
}

export enum Type {
    OUTSIDE = 'Внешнее',
    INSIDE = 'Внутреннее',
}

export enum Direction{
  NID = "НИД",
  KTD ="КТД",
  UD = "УД",
  SD = "СД",
  OD = "ОД",
}